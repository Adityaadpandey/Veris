// @walletconnect/react-native-compat ships no types — it's a side-effect-only polyfill import
// (patches crypto/WebSocket/storage globals for React Native), so there's nothing to type.
declare module '@walletconnect/react-native-compat';
